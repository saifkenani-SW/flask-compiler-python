package ast.template.html;

public class HTMLVoidElementNode extends HTMLElementNode {
    public HTMLVoidElementNode(int line, int column, String tagName) {
        super(line, column, tagName);
    }
}
