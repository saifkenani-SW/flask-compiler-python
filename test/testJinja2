<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}تطبيق المنتجات{% endblock %}</title>

    <style class="h" media="all">
body {
    font-family: 'Arial';
    background-color: #f4f4f4;
    color: #333;
    margin: 0;
    padding: 0;
    text-align: right; /* دعم اللغة العربية */
    direction: rtl;
}

header {
    background-color: #007bff;
    color: white;
    padding: 20px;
    text-align: center;
}

header h1 {
    margin: 0 0 10px 0;
}

nav a {
    color: white;
    margin: 0 15px;
    text-decoration: none;
    font-weight: bold;
}

main {
    max-width: 1200px;
    margin: 20px auto;
    padding: 20px;
    background-color: white;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

/* تصميم قائمة المنتجات (index.html) */
.product-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
    justify-content: center;
    padding: 20px 0;
}

.product-card {
    border: 1px solid #ddd;
    padding: 15px;
    width: 300px;
    box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.05);
    text-align: center;
    background-color: #fff;
}

.product-image {
    width: 100%;
    height: 200px;
    object-fit: cover;
    margin-bottom: 10px;
}

.product-price {
    color: #28a745;
    font-size: 1.2em;
    font-weight: bold;
}

.details-button, .delete-button {
    display: block;
    width: 100%;
    padding: 10px;
    margin-top: 10px;
    text-decoration: none;
    border: none;
    cursor: pointer;
    font-weight: bold;
    text-align: center;
}

.details-button {
    background-color: #007bff;
    color: white;
}

.delete-button {
    background-color: #dc3545;
    color: white;
}

/* تصميم تفاصيل المنتج (product_detail.html) */
.detail-container {
    max-width: 800px;
    margin: 0 auto;
}

.detail-image {
    width: 100%;
    max-height: 400px;
    object-fit: contain;
    margin-bottom: 20px;
    border: 1px solid #ddd;
}

.description-text {
    border-right: 3px solid #007bff;
    padding-right: 10px;
    margin-top: 10px;
}

.back-link {
    display: inline-block;
    margin-top: 20px;
    color: #007bff;
    text-decoration: none;
    font-weight: bold;
}

/* تصميم نموذج الإضافة (add_product.html) */
.product-form label {
    display: block;
    margin-top: 15px;
    margin-bottom: 5px;
    font-weight: bold;
}

.product-form textarea {
    width: 98%;
    padding: 10px;
    margin-bottom: 10px;
    border: 1px solid #ccc;
    box-sizing: border-box; /* للتأكد من أن padding لا يزيد العرض الكلي */
}

.submit-button {
    background-color: #28a745;
    color: white;
    padding: 12px 20px;
    border: none;
    cursor: pointer;
    font-size: 1.1em;
    margin-top: 20px;
    width: 100%;
}

.no-products {
    text-align: center;
    padding: 50px;
    color: #6c757d;
    font-style: italic;
}
    </style>
</head>
<body>
<header>
    <h1 style="">متجر التجارة الإلكترونية المصغر</h1>
    <nav>
        <a href="{{ url_for('product_list') }}">عرض المنتجات</a>
        <a href="{{ url_for('add_product') }}">إضافة منتج</a>
    </nav>
</header>

<main>
    {% block content %}
    <p>محتوى افتراضي.</p>
    {% endblock %}
</main>

<footer>
    <p>&copy; 2025 Flask & Jinja2 App - محمد وسيم البزرة</p>
</footer>
</body>
</html>
<!DOCTYPE html>
<html lang="ar" dir="rtl">
{% extends "base.html" %}
{% block title %}إضافة منتج جديد{% endblock %}

{% block content %}
<h2>إضافة منتج جديد</h2>

<form method="POST" action="{{ url_for('add_product') }}" class="product-form" enctype="multipart/form-data">

    <label for="name">اسم المنتج:</label>
    <input type="text" id="name" name="name" required>

    <label for="price">سعر المنتج ($):</label>
    <input type="number" id="price" name="price" step="0.01" min="0.01" required>

    <label for="details">التفاصيل / الوصف:</label>
    <textarea id="details" name="details" rows="5" required></textarea>

    <label for="image">صورة المنتج:</label>
    <input type="file" id="image" name="image_file" accept=".jpg, .jpeg, .png, .gif" required>

    <button type="submit" class="submit-button">حفظ وإضافة المنتج</button>
</form>
{% endblock %}
</html>
<!DOCTYPE html>
<html lang="ar" dir="rtl">

{% extends "base.html" %}

{% block title %}
قائمة المنتجات
{% endblock %}

{% block content %}
<h2 style="text-align:center;">قائمة المنتجات</h2>

{% if products %}
<div class="product-grid">
    {% for product in products %}
    <div class="product-card">
        <img src="{{ product.image_url }}"
             alt="صورة {{ product.name }}"
             class="product-image">

        <h3>{{ product.name }}</h3>

        <p class="product-price">
            {{ product.price }} $
        </p>

        <a href="{{ url_for('product_detail', product_id=product.id) }}"
           class="details-button">
            عرض التفاصيل
        </a>

        <form action="{{ url_for('delete_product', product_id=product.id) }}"
              method="post">
            <button type="submit" class="delete-button">
                حذف المنتج
            </button>
        </form>
    </div>
    {% endfor %}
</div>
{% else %}
<p class="no-products">
    لا توجد منتجات حالياً.
</p>
{% endif %}

{% endblock %}

</html>
<!DOCTYPE html>
<html lang="ar" dir="rtl">
{% extends "base.html" %}
{% block title %}تفاصيل: {{ product.name }}{% endblock %}

{% block content %}
<div class="detail-container">
    <h2>تفاصيل المنتج</h2>

    <img src="{{ product.image_url }}" alt="صورة {{ product.name }}" class="detail-image">

    <div class="info-block">
        <p><strong>اسم المنتج:</strong> {{ product.name }}</p>
        <p><strong>السعر:</strong> {{ product.price }} $</p>
        <p><strong>الوصف:</strong></p>
        <p class="description-text">{{ product.details }}</p>
    </div>

    <a href="{{ url_for('product_list') }}" class="back-link">العودة إلى القائمة الرئيسية</a>
</div>
{% endblock %}
</html>
