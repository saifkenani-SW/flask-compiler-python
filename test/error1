# test_error1.py
from flask import Flask

app = Flask(__name__)

# أخطاء في الرموز
@app route('/')  # ❌ ناقص نقطة
def hello()
    return "Hello"  # ❌ ناقص نقطتين

if __name__ == "__main__"
    app.run(debug=True)  # ❌ ناقص نقطتين